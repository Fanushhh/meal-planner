# Local Claude Notes

## Chrome DevTools MCP
Requires Google Chrome at `/opt/google/chrome/chrome`. On this machine only Chromium is installed (`/usr/bin/chromium`).
Fix: `sudo mkdir -p /opt/google/chrome && sudo ln -s /usr/bin/chromium /opt/google/chrome/chrome`
